# 安全策略

## 支持范围

当前仓库维护 `v1.0.x` 与默认分支；历史阶段快照用于工程证据，不单独提供安全修复。

| 版本 | 安全更新 |
| --- | --- |
| `v1.0.x` / 默认分支 | 支持 |
| 历史阶段快照 | 不支持 |
| 未来《素简记 Pro》 | 不属于本仓库 |

## 报告安全问题

仓库公开后，优先使用 GitHub Private Vulnerability Reporting 私下报告。若该入口尚未
启用，可以只创建一条不含漏洞细节的 Issue，请求仓库所有者提供私密沟通渠道。

不要在公开 Issue、Discussion、截图或日志中提交：

- 密码、Token、Cookie、私钥、数据库或中间件凭据；
- 可直接利用的完整攻击步骤；
- 用户个人信息、订单、地址、支付或退款数据；
- 本机代理订阅、内网拓扑密钥或私有仓库地址。

收到报告后，仓库所有者会先确认影响范围，再决定修复、临时缓解和披露时间。本个人
项目不承诺商业 SLA，但不会把中间件异常、结果未知或安全失败伪造成成功。

## 演示账号边界

`reader@example.com`、`reader-two@example.com` 和 `admin@example.com` 是本地
Mock API 的公开夹具账号，不是真实生产凭据。演示环境不能连接真实客户数据或复用
生产密钥。
